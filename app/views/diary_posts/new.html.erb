<div class="container" style="max-width: 600px;">
  <h1 class="font-weight-normal mb-4">投稿（日記）</h1>
  <p class="text-muted small mb-3">ここから投稿すると、<strong>あなただけが閲覧できる日記</strong>に保存されます。下書きも可能です。</p>

  <% if @post.errors.any? %>
    <div class="alert alert-danger">
      <%= @post.errors.count %>件のエラーが発生しました
      <% @post.errors.full_messages.each do |message| %>
        <div><%= message %></div>
      <% end %>
    </div>
  <% end %>

  <%= form_with model: @post, url: diary_posts_path, local: true, html: { multipart: true } do |f| %>
    <div class="mb-3">
      <%= f.label :location, "やったこと", class: "form-label" %>
      <%= f.text_field :location, class: "form-control", placeholder: "例：運動" %>
    </div>

    <div class="mb-3">
      <%= f.label :text, "感想", class: "form-label" %>
      <%= f.text_area :text, rows: 6, class: "form-control", placeholder: "例：ピラティスに入会した" %>
    </div>

    <div class="mb-3">
      <%= f.label :image, "画像", class: "form-label" %>
      <%= f.file_field :image, class: "form-control", accept: "image/*" %>
    </div>

    <div class="mt-4">
      <%= f.submit "投稿する", name: "save_as", value: "投稿する", class: "btn btn-primary" %>
      <%= f.submit "下書き保存", name: "save_as", value: "下書き保存", class: "btn btn-secondary ms-2" %>
    </div>
  <% end %>
</div>
